<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Wizard Form View -->
    <record id="view_task_wizard_form" model="ir.ui.view">
        <field name="name">task.wizard.form</field>
        <field name="model">task.wizard</field>
        <field name="arch" type="xml">
            <form string="Batch Update Tasks">
                <group>
                    <field name="new_state" widget="radio"/>
                </group>
                <footer>
                    <button string="Apply" name="action_apply" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Wizard Action (Target New = Popup) -->
    <record id="action_task_wizard" model="ir.actions.act_window">
        <field name="name">Batch Update State</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">task.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="model_task_task"/>
        <field name="binding_view_types">list</field>
    </record>
</odoo>
